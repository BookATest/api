EligibleAnswer:
  type: "object"
  properties:
    id:
      type: "integer"
    question_id:
      type: "integer"
    answer:
      type: "object"
      properties:
        type:
          type: "string"
          enum: [">", "<"]
        interval:
          type: "integer"

EligibleAnswerCollection:
  type: "array"
  items:
    $ref: "#/EligibleAnswer"

CreateEligibleAnswer:
  type: "object"
  properties:
    question_id:
      type: "integer"
    answer:
      type: "object"
      properties:
        type:
          type: "string"
          enum: [">", "<"]
        interval:
          type: "integer"

EditEligibleAnswer:
  $ref: "#/CreateEligibleAnswer"

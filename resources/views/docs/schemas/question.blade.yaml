ModifyQuestion:
  type: "object"
  required:
    - "question"
    - "type"
  properties:
    question:
      type: "string"
    type:
      type: "string"
      enum: ["select", "checkbox", "date", "text"]
    options:
      type: "array"
      items:
        $ref: "/docs/schemas/question-option.yaml#/QuestionOption"
      nullable: true

Question:
  type: "object"
  properties:
    id:
      type: "integer"
    question:
      type: "string"
    type:
      type: "string"
      enum: ["select", "checkbox", "date", "text"]
    options:
      type: "array"
      items:
        $ref: "/docs/schemas/question-option.yaml#/QuestionOption"

QuestionCollection:
  type: "object"
  properties:
    data:
      type: "array"
      items:
        $ref: "#/Question"
    links:
      $ref: "/docs/schemas/api-resource.yaml#/ApiResourceLinks"
    meta:
      $ref: "/docs/schemas/api-resource.yaml#/ApiResourceMeta"
